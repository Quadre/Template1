<UserControl x:Class="Template1.Controls.LeftMenuControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Template1.Controls"
             mc:Ignorable="d" 
             Background="Aqua"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid MinWidth="200">
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Menu" TextElement.FontSize="16" Margin="15 0 "/>
            <Button Content="Dashboard" />
            <Button Content="Outstanding Queries" />
            <Button Content="Notifications" />
            <Button Content="Teams" />
            <Button Content="Users" />
        </StackPanel>
        <Border Grid.Row="1" CornerRadius="15" Background="White" Margin="25 0">
            <TextBox Margin="20 5" Text="Search" BorderThickness="1" BorderBrush="White"/>
        </Border>
    </Grid>
</UserControl>
